<!DOCTYPE html>
<html>
	<head>
		<title>Course Info</title>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
	</head>
	<body>
		<nav class="navbar navbar-fixed-top" style="top:50px;background:#D8D8D8;height:70px;">
			<br>
			<div class="container">
				<div class="nav navbar-nav">
					<h4> <b> Course Info at IIT BOMBAY (Filling Teaching Course)</b> </h4>
				</div>
			</div>
		</nav>
		
		<div class="container" style="padding-top:75px;">
			<%= form_for @user, :class=>'form-horizontal' do |f| %>
				<% if @user.errors.any? %>
					<div class="error_messages">
						<h2>Form is invalid</h2>
						<ul>
							<% @user.errors.full_messages.each do |message| %>
								<li><%= message %></li>
							<% end %>
						</ul>
					</div>
				<% end %>
				<div class="form-group">
					<%= f.label :course, "Teaching Course", :for=>"select_fac", :class=>"col-sm-2 control-label"%> 
					<div class="col-sm-10">
						<%= f.select :course, options_for_select([*Article.all.map(&:course_code)]), {}, {:class=>'form-control', :id=>'select_fac'} %><br/><br/>
					</div>
				</div>
				<div class="form-group">
					<div class="col-sm-offset-2 col-sm-10">
						<%= f.submit "Update teaching course", :class=>"btn btn-success"%> <br/> <br/>
						<%= link_to "Go back to Courses", root_url, :class=>"btn btn-danger", :role=>"button" %><br/><br/><br/>
					</div>
				</div>
			<%end%>
		</div>
	</body>
</html>
